@{
    Html.RenderPartial(
        (
        HttpContext.Current.Session["CompanyID"] != null ||
        HttpContext.Current.Session["CustomerID"] != null
        ) ? "_RegisteredUser" : "_UnRegisteredUser");
}